{"format_version":"1.8.0",
	"animation_controllers": {
		"controller.animation.dm61_equip_1": {
			"states": {
				"default": {
					"transitions": [
						{ "att": "query.is_powered && !variable.has_target" }

					]

				},
				"att": {
					"transitions": [
						{
							"default": "!query.is_powered || variable.has_target"
						}
					],
					"on_entry": [
						"@s silent"
					]
				}
			}
		}
	}
}