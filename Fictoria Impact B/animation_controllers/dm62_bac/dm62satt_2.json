{"format_version":"1.8.0",
"animation_controllers":{
"controller.animation.dm62satt_2":{
"states":{
"default":{"transitions":[
	{ "attack": "query.health <= 74 && query.health > 63 && query.variant == 2" }

]

},
"attack": {
					"transitions": [
						{
							"default": "query.health > 74 || query.health <= 63 || query.variant != 2"
						}
					],
	"on_entry": [
		"@s sa2",
		"/effect @s absorption 0",
		"/effect @s resistance 0"
	]
}
}
}
}
}