{"format_version":"1.8.0",
"animation_controllers":{
"controller.animation.dm62satt_6":{
"states":{
"default":{"transitions":[
	{ "attack": "query.health <= 36 && query.health > 27 && query.variant == 2" }

]

},
"attack": {
					"transitions": [
						{
							"default": "query.health > 36 || query.health <= 27 || query.variant != 2"
						}
					],
	"on_entry": [
		"@s sa6",
		"/effect @s absorption 0"
	]
}
}
}
}
}