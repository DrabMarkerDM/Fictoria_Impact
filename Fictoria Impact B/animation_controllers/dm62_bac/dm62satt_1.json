{"format_version":"1.8.0",
"animation_controllers":{
"controller.animation.dm62satt_1":{
"states":{
"default":{"transitions":[
	{ "attack": "query.health > 72 && query.variant == 2" }

]

},
"attack": {
					"transitions": [
						{
							"default": "query.health <= 72 || query.variant != 2"
						}
					],
	"on_entry": [
		"@s sa1",
		"/effect @s absorption 0",
		"/effect @s resistance 0"
	]
}
}
}
}
}