{"format_version":"1.8.0",
"animation_controllers":{
"controller.animation.dm62satt_7":{
"states":{
"default":{"transitions":[
	{ "attack": "query.health <= 27 && query.health > 18 && query.variant == 2" }

]

},
"attack": {
					"transitions": [
						{
							"default": "query.health > 27 || query.health <= 18 || query.variant != 2"
						}
					],
	"on_entry": [
		"@s sa7",
		"/effect @s absorption 0"
	]
}
}
}
}
}