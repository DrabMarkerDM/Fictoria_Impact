{"format_version":"1.8.0",
"animation_controllers":{
"controller.animation.dm62att_5":{
"states":{
"default":{"transitions":[
	{ "attack": "query.health <= 45 && query.health > 36 && query.variant == 1" }

]

},
"attack": {
					"transitions": [
						{
							"default": "query.health > 45 || query.health <= 36 || query.variant != 1"
						}
					],
	"on_entry": [
		"@s a5",
		"/effect @s absorption 0",
		"/effect @s resistance 0"
	]
}
}
}
}
}