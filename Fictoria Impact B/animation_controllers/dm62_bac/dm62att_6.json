{"format_version":"1.8.0",
"animation_controllers":{
"controller.animation.dm62att_6":{
"states":{
"default":{"transitions":[
	{ "attack": "query.health <= 36 && query.health > 27 && query.variant == 1" }

]

},
"attack": {
					"transitions": [
						{
							"default": "query.health > 36 || query.health <= 27 || query.variant != 1"
						}
					],
	"on_entry": [
		"@s a6",
		"/effect @s absorption 0"
	]
}
}
}
}
}