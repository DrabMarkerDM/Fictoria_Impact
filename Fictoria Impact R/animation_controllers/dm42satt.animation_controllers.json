{
	"format_version" : "1.10.0",
	"animation_controllers" : {
		"controller.animation.dm42satt" : {
			"initial_state" : "default",
			"states" : {
				"attacking" : {
					"animations" : [ "charging" ],
					"transitions" : [
						{
							"default" : "query.is_roaring == 0"
						}
					]
				},
				"default" : {
					"transitions" : [
						{
							"attacking" : "query.is_roaring == 1"
						}
          ]
        }
      }
    }
  }
}
