{
	"format_version" : "1.10.0",
	"animation_controllers" : {
		"controller.animation.bomb_soldier" : {
		             			"initial_state" : "default",
			"states" : {
				"ratt" : {
					"animations" : [ "bow_and_arrow" ],
					"transitions" : [
						{
							"default": "!query.has_target || query.variant != 0"
						}
					]
				},
				"default" : {
					"transitions" : [
						{
							"ratt": "query.has_target && query.variant == 0"
						}
          ]
        }
      }
    }
  }
}
