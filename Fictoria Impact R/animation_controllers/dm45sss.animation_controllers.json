{
	"format_version" : "1.10.0",
	"animation_controllers" : {
		"controller.animation.dm45sss" : {
			"initial_state" : "default",
			"states" : {
				"attacking" : {
					"animations" : [ "charging" ],
					"transitions" : [
						{
							"default" : "!query.variant == 2 || !query.has_target"
						}
					]
				},
				"default" : {
					"transitions" : [
						{
							"attacking" : "query.variant == 2 && query.has_target"
						}
          ]
        }
      }
    }
  }
}
