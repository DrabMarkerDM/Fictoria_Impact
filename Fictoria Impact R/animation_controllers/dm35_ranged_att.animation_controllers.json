{
	"format_version" : "1.10.0",
	"animation_controllers" : {
		"controller.animation.dm35" : {
		             			"initial_state" : "default",
			"states" : {
				"ratt" : {
					"animations" : [ "bow_and_arrow" ],
					"transitions" : [
						{
							"default" : "!query.has_target || query.get_equipped_item_name != 'dm35_bow'"
						}
					]
				},
				"default" : {
					"transitions" : [
						{
							"ratt" : "query.has_target && query.get_equipped_item_name == 'dm35_bow'"
						}
          ]
        }
      }
    }
  }
}
